meta {
  name: Delete Book By ID
  type: http
  seq: 8
}

delete {
  url: {{host}}/books/{{createdBookId}}
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

assert {
  res.body: isEmpty
  res.status: eq 204
}
