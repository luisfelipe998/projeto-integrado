meta {
  name: Delete User by Id
  type: http
  seq: 4
}

delete {
  url: {{host}}/users/{{createdUserId}}
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

assert {
  res.body: isEmpty
  res.status: eq 204
}
