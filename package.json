{
  "name": "library_api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run clear && npx tsc",
    "clear": "rm -rf dist",
    "start:local": "npm run build && node dist/index.js",
    "start:local:container": "docker compose up --build -d",
    "stop:local:container": "docker compose down",
    "test:local": "jest --config jest.unit.config.js --coverage",
    "test:local:integration": "jest --config jest.integration.config.js --coverage",
    "test:local:e2e": "npm run stop:local:container && npm run start:local:container && mkdir -p artifacts/e2e && cd ./tests/e2e/collection/library_api && sleep 5 && npx bru run --env local --reporter-html results.html && cd ../../../../ && mv ./tests/e2e/collection/library_api/results.html ./artifacts/e2e/results.html && npm run stop:local:container",
    "lint": "npx eslint src/**/*.ts",
    "lint:fix": "npx eslint src/**/*.ts --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@testcontainers/postgresql": "^11.7.2",
    "@types/express": "^5.0.2",
    "@types/jest": "^30.0.0",
    "@types/module-alias": "^2.0.4",
    "@types/node": "^22.7.2",
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^8.46.2",
    "@typescript-eslint/parser": "^8.46.2",
    "eslint": "^9.39.0",
    "jest": "^30.2.0",
    "module-alias": "^2.2.3",
    "testcontainers": "^11.7.2",
    "ts-jest": "^29.4.5",
    "typescript": "^5.6.2"
  },
  "_moduleAliases": {
    "@": "./dist"
  },
  "dependencies": {
    "@usebruno/cli": "^2.13.2",
    "express": "^5.1.0",
    "postgres": "^3.4.7"
  }
}
